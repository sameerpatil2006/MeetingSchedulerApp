package com.scheduler.scheduler;

import com.scheduler.scheduler.data.MeetingResponse;

public interface OnListFragmentInteractionListener {
    void onListFragmentInteraction(MeetingResponse item);
}
